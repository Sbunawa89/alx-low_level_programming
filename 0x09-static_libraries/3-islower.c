#include "main.h"


/**
* _islower - Wrirting a program to check for lower case letter
* @c: the character to check
* Return: 1 for lowercase, 0 otherwise
*/

int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
